{
  "name": "robotics_plath_planning",
  "version": "1.0.0",
  "description": "This cpp service is responsible for robot path planning, it handles the state and commands the robot to move where",
  "main": "index.js",
  "scripts": {
    "prebuild": "rimraf build",
    "build": "tsc && node build/src/main.js",
    "test": "rimraf build && tsc && jest",
    "test-e2e": "node test/e2e-test.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "astar-typescript": "^1.2.7",
    "canvas": "^2.11.2",
    "express": "^4.18.2",
    "jest": "^29.7.0",
    "socket.io": "^4.7.4",
    "ts-node": "^10.9.2"
  },
  
  "devDependencies": {
    "@types/jest": "^29.5.11",
    "ts-jest": "^29.1.2"
  }
}
